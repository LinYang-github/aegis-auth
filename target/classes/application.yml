spring:
  application:
    name: aegis-auth
  profiles:
    active: lite # 默认为 lite (SQLite + Caffeine)
    # active: prod # 切换为 prod (MySQL + Redis)

# 通用配置
server:
  port: 9000

logging:
  level:
    org.springframework.security: TRACE
    org.springframework.security.oauth2: TRACE